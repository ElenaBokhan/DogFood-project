export enum ERoute {
    TO_BACK,
    TO_HOME,
}

// const UseCustomNavigation = (route: ERoute) => {
//     const navigate = useNavigate();
//
//     switch (route) {
//         case ERoute.TO_BACK:
//             return navigate(-1);
//         case ERoute.TO_HOME:
//             return navigate('/');
//     }
// };
